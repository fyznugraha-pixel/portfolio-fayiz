# Custom Agent Rules

- **Git Push Policy**: Do NOT automatically run `git push` after making commits. Only run `git push` when the user explicitly instructs you to do so (e.g., "push", "coba push", "tolong push"). Always leave the changes committed locally and wait for confirmation.
